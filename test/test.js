const express = require('express');
const chai = require('chai');
const request = require('supertest');
const app = express();
describe('POST Create requirement', () => {
    it('should create wallet for the user', () => {
        // code for testing the api
});
});